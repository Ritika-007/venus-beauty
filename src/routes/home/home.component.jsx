import Directory from "../../components/directory/directory.component";

// Home component serves as a container for rendering the Directory component, which is responsible for displaying the directory or
// menu of categories or sections on the homepage the application.
 
const Home = () => {
  return (
    <Directory />
  )
}

export default Home
